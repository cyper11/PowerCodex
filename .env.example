# Server-only PostgreSQL URL. Use your provider's pooled URL for deployed requests.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/powercodex?sslmode=require

# Optional direct connection for migrations. Defaults to DATABASE_URL if unset.
# DATABASE_DIRECT_URL=postgresql://USER:PASSWORD@HOST:5432/powercodex?sslmode=require
