'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {useState,useEffect} from 'react';
import {Landmark,Menu,X,ArrowUpRight,BookOpen,UserRound,Settings,Search} from 'lucide-react';
export function Shell({children}:{children:React.ReactNode}){const pathname=usePathname();useEffect(()=>{try{document.documentElement.classList.toggle('large-text',localStorage.getItem('codex-large-text')==='true')}catch{}},[]);const [open,setOpen]=useState(false);const items=[['/','Home'],['/laws','The 48 Laws'],['/analyzer','Analyzer'],['/simulator','Simulator']];return <><a className="skip" href="#main">Skip to content</a><header className="header"><Link className="brand" href="/" aria-label="The Power Codex home"><Landmark size={27}/><span>POWER CODEX<small>KNOWLEDGE · STRATEGY · INFLUENCE</small></span></Link><nav className={open?'main-nav open':'main-nav'} aria-label="Main navigation">{items.map(([url,label])=><Link key={url} className={pathname===url||url!=='/'&&pathname.startsWith(url)?'active':''} href={url} onClick={()=>setOpen(false)}>{label}</Link>)}<Link className="mobile-only" href="/journal" onClick={()=>setOpen(false)}>Journal</Link><Link className="mobile-only" href="/profile" onClick={()=>setOpen(false)}>Power profile</Link><Link className="mobile-only" href="/settings" onClick={()=>setOpen(false)}>Settings</Link></nav><div className="nav-tools"><Link href="/laws?focus=search" aria-label="Search laws"><Search size={18}/></Link><Link href="/journal" className={pathname==='/journal'?'active':''} aria-label="Power journal"><BookOpen size={18}/></Link><Link href="/profile" className={pathname==='/profile'?'active':''} aria-label="Power profile"><UserRound size={18}/></Link><Link href="/settings" aria-label="Settings and about"><Settings size={18}/></Link></div><button className="menu-button" onClick={()=>setOpen(!open)} aria-label={open?'Close menu':'Open menu'} aria-expanded={open}>{open?<X/>:<Menu/>}</button></header><main id="main">{children}</main><footer><Link className="footer-brand" href="/">THE POWER CODEX</Link><span>Isang independent na study companion sa work ni Robert Greene.</span><Link href="/settings">About the project <ArrowUpRight size={14}/></Link></footer></>}
export function Eyebrow({children}:{children:React.ReactNode}){return <div className="eyebrow"><span/>{children}</div>}
export function PageHead({kicker,title,description,action}:{kicker:string,title:string,description?:string,action?:React.ReactNode}){return <div className="page-head"><div><Eyebrow>{kicker}</Eyebrow><h1>{title}</h1>{description&&<p>{description}</p>}</div>{action}</div>}
